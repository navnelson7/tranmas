import React from "react";
import { Fragment } from "react";
const Refrenda = (refrenda, id) => {
  return (
    <Fragment>
      <td>{refrenda.id}</td>
    </Fragment>
  );
};

export default Refrenda;
